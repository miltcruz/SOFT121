// Centralized API endpoints
const BASE_URL = 'http://localhost:5156';

const ENDPOINTS = {
  PRODUCT: `${BASE_URL}/product`,
  LOGIN: `${BASE_URL}/login`,
  WEATHER: `${BASE_URL}/weatherforecast`,
};

export { BASE_URL, ENDPOINTS };
